<Page x:Class="DrillApp.View.Pages.Equipment.TypeEquipmentAddEditPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DrillApp.View.Pages.Equipment"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TypeEquipmentAddEditPage">

    <Grid>
        <StackPanel HorizontalAlignment="Center" Width="400" VerticalAlignment="Center">
            <TextBlock Text="Наименование" />
            <TextBox Text="{Binding Имя}" />
            <TextBlock Text="Масса" />
            <TextBox Text="{Binding Масса}" />
            <TextBlock Text="Частота" />
            <TextBox Text="{Binding Частота}"/>
            <TextBlock Text="Число двигателей" />
            <TextBox Text="{Binding ЧислоДвигателей}"/>
            <TextBlock Text="Условная глубина" />
            <TextBox Text="{Binding УсловнаяГлубина}"/>

            <Button Click="SaveClick" Content="Сохранить" />

        </StackPanel>
    </Grid>
</Page>
